using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AvatarChanger : MonoBehaviour
{
    //GameObject variableForPrefab;
    //public GameObject c1;
    //string avatarName, avatar = "avatarChoice";

    void Awake()
    {
       
    }
    // Start is called before the first frame update
    void Start()
    {
        //avatarName = PlayerPrefs.GetString(avatar);
        //Debug.Log("avatar placed: " + avatarName);
        //Instantiate(Resources.Load(avatarName, typeof(GameObject)) as GameObject);
        ////Instantiate(c1);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
